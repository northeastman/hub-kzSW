# 时序图布局参考

## 适用场景

用于表达多个角色或服务随时间发生的调用和返回。

## 布局

1. 参与者从左到右排列，时间从上到下。
2. 每个参与者下方绘制垂直虚线生命线。
3. 同步调用使用实线实心箭头。
4. 返回消息使用虚线开放箭头。
5. 消息较多时加序号，如 `1. createOrder()`。
6. 可选步骤用半透明分组框，并标注条件。

消息标签保持简短，箭头之间至少留 34px 垂直距离。

